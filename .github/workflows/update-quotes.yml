name: Update market quotes

on:
  workflow_dispatch:
  schedule:
    - cron: '*/5 * * * *'

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.x'
      - name: Fetch quotes
        run: python oanda_api/update_quotes.py
      - name: Commit updated quotes
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add oanda_api/quotes.json
          git diff --cached --quiet || git commit -m "Update market quotes"
          git push
