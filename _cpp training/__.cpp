
given: sequence a[N]     N <= 100,000 // logarithms does not matter too much
decrease one given element by A and remaining N - 1 elements by B
objective : what is the minimum number of cycles to make all values neg

// binary search ?
// SEQUENCE a[i]
a[i] -= B*K
// K times we choose an element to decrease by (A-B)

case 1. A >= B

int main() {
    int n;
vector<char> temperature(n);

n - 1 B
for(i = n; i - 1; i++) {




    (i = 1; )
    }

    std::cout << " " << std::
}

/*
#include <iostream>
#include <limits>

int main() {
    std::cout  << std::numeric_limits<int>::max() << '\n';
    std::cout  << std::numeric_limits<int>::min() << '\n';
    std::cout  << std::numeric_limits<int>::min() << '\n';

    return 0;
} */
